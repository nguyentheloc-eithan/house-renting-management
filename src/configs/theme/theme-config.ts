import type { ThemeConfig } from 'antd';

const themeConfig: ThemeConfig = {
  token: {
    // borderRadius: 8,
    colorPrimary: ' #005ff7',
    fontSize: 14,
  },
  components: {},
};

export default themeConfig;
